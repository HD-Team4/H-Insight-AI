# Grafana 데이터소스 자동 프로비저닝
# uid: prometheus 로 고정 → 대시보드 JSON 이 이 uid 로 데이터소스를 참조한다.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://prometheus:9090   # 같은 compose 네트워크의 prometheus 서비스
    isDefault: true
    editable: true
