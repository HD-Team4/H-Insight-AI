# 모니터링 스택(Grafana/Prometheus)용 환경변수 예시
# 실제 사용 시 이 파일을 monitoring/.env 로 복사해서 값 채우기
#   cp monitoring/.env.example monitoring/.env

# Grafana 관리자 비밀번호 (미설정 시 기본 admin)
GRAFANA_ADMIN_PASSWORD=changeme
